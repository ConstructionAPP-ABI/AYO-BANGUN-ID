Ayo Bangun Firebase Array Normalize Fix

Perbaikan: normalisasi data Realtime Database agar array projects/users tetap terbaca sebagai array setelah Firebase load.
Tidak mengubah Rules dan tidak menghapus data Firebase.
