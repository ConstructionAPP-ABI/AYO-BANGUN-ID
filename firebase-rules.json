{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "abpos": {
      "state": {
        ".read": "auth != null",
        ".write": "auth != null",
        ".validate": "newData.hasChildren(['users','projects','data','settings','meta'])"
      }
    }
  }
}
